{% for dossier in dossiers %}
    <li class="nav-item">
        <a class="nav-link active" aria-current="page" href="{{ path("app_dossier", {"nomDuDossier": dossier.filename}) }}">{{ dossier.filename }}</a>
    </li>
{% endfor %}