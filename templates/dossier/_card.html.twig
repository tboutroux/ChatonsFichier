<div class="card" style="width: 20rem;">
	<img src="{{ asset(fichier.pathname) }}" style="aspect-ratio:1/1; object-fit: cover;">
	<div class="card-body">
		<h5 class="card-title">{{ fichier.filename }}</h5>
		<p class="card-text">
			Taille de l'image :
			{{ fichier.size }}
			octets
			<br>
			Date de création :
			{{ fichier.ctime|date("d/m/Y") }}
		</p>
	</div>
</div>
